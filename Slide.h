#pragma once
#include <gl/glm/glm/glm.hpp>
#include <vector>
#include "Model.h"

// 정육면체 자르기
void sliceBox(const Model& box) {
	return;
}

// 구 자르기
void sliceSphere(const Model& sphere) {
	return;
}

// 실린더 자르기
void sliceCylinder(const Model& cylinder) {
	return;
}